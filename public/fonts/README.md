# Fonts Directory

Place `NewYork_font.otf` in this directory for the Opacity app to use the New York font for large titles.

The font is configured in `/app/opacity/page.tsx` and will fallback to the system font if not available.
